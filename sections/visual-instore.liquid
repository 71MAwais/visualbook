{% comment %} sections/visual-instore.liquid {% endcomment %}

<style>
    /* Hide scrollbars but allow scrolling */
    .hide-scrollbar::-webkit-scrollbar { display: none; }
    .hide-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    
    /* Drag to scroll interactive logic */
    .drag-scroll {
        overflow-x: auto;
        scroll-behavior: auto;
        -webkit-overflow-scrolling: touch;
        cursor: grab;
    }
    .drag-scroll:active {
        cursor: grabbing !important;
    }
    .drag-scroll.is-dragging {
        user-select: none;
    }
    .drag-scroll.is-dragging * {
        pointer-events: none; /* Prevents text selection while dragging */
    }
</style>

<section class="py-12 md:py-16 bg-white overflow-hidden" id="instore-{{ section.id }}">
    {% if section.settings.title != blank %}
        <p class="text-center text-[11px] md:text-[13px] font-sans font-bold tracking-[0.3em] uppercase text-[#B8906B] mb-10 md:mb-14">
            {{ section.settings.title }}
        </p>
    {% endif %}
    
    <div class="relative w-full" aria-hidden="true">
        <div class="absolute top-0 left-0 w-12 md:w-24 h-full bg-gradient-to-r from-white to-transparent z-10 pointer-events-none"></div>
        <div class="absolute top-0 right-0 w-12 md:w-24 h-full bg-gradient-to-l from-white to-transparent z-10 pointer-events-none"></div>
        
        <div id="instore-scroll-{{ section.id }}" class="drag-scroll hide-scrollbar w-full" style="overscroll-behavior-x: none;">
            
            <div class="flex shrink-0 items-center">
                
                {% capture store_names %}
                    <div class="instore-group flex shrink-0 items-center justify-around min-w-max gap-12 md:gap-24 px-6 md:px-12 h-20 whitespace-nowrap">
                        {% for block in section.blocks %}
                            
                            <div class="flex items-center justify-center shrink-0" {{ block.shopify_attributes }}>
                                {% if block.settings.logo != blank %}
                                    <img src="{{ block.settings.logo | image_url: height: 120 }}" alt="{{ block.settings.store_name }}" class="h-10 md:h-14 lg:h-16 w-auto object-contain" loading="lazy">
                                {% else %}
                                    <span class="font-serif text-[36px] md:text-[48px] lg:text-[56px] text-stone-950 font-medium tracking-tight">{{ block.settings.store_name }}</span>
                                {% endif %}
                            </div>
                            
                            <span class="text-[#B8906B] mx-2 md:mx-6 text-lg md:text-2xl">✦</span>
                            
                        {% endfor %}
                    </div>
                {% endcapture %}

                {% for i in (1..6) %}
                    {{ store_names }}
                {% endfor %}
                
            </div>
        </div>
    </div>
</section>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const slider = document.getElementById('instore-scroll-{{ section.id }}');
        if (!slider) return;

        const group = slider.querySelector('.instore-group');
        let isDown = false;
        let startX;
        let scrollLeft;
        let isAnimating = true;
        let animationId;
        const speed = {{ section.settings.scroll_speed | default: 1 }};

        // 1. Automatic Infinite Scrolling Logic
        function autoScroll() {
            if (isAnimating && group) {
                slider.scrollLeft += speed;
                const groupWidth = group.offsetWidth;
                
                // Seamlessly jump back/forward when we scroll past a full group's width
                if (slider.scrollLeft >= groupWidth * 2) {
                    slider.scrollLeft -= groupWidth;
                } else if (slider.scrollLeft <= 0) {
                    slider.scrollLeft += groupWidth;
                }
            }
            animationId = requestAnimationFrame(autoScroll);
        }

        // Start the loop
        autoScroll();

        // 2. Desktop Mouse Interaction
        slider.addEventListener('mousedown', (e) => {
            isDown = true;
            isAnimating = false; // Pause auto-scroll on grab
            slider.classList.add('is-dragging');
            startX = e.pageX - slider.offsetLeft;
            scrollLeft = slider.scrollLeft;
        });
        slider.addEventListener('mouseleave', () => {
            isDown = false;
            isAnimating = true; // Resume auto-scroll when mouse leaves
            slider.classList.remove('is-dragging');
        });
        slider.addEventListener('mouseup', () => {
            isDown = false;
            isAnimating = true; // Resume auto-scroll on release
            slider.classList.remove('is-dragging');
        });
        slider.addEventListener('mousemove', (e) => {
            if (!isDown) return;
            e.preventDefault();
            const x = e.pageX - slider.offsetLeft;
            const walk = (x - startX) * 2; // Multiply by 2 for faster drag scrolling
            slider.scrollLeft = scrollLeft - walk;
        });

        // 3. Mobile Touch Interaction
        slider.addEventListener('touchstart', (e) => {
            isDown = true;
            isAnimating = false; // Pause auto-scroll on touch
            startX = e.touches[0].pageX - slider.offsetLeft;
            scrollLeft = slider.scrollLeft;
        }, {passive: true});
        slider.addEventListener('touchend', () => {
            isDown = false;
            isAnimating = true; // Resume auto-scroll on release
        });
        slider.addEventListener('touchmove', (e) => {
            if (!isDown) return;
            const x = e.touches[0].pageX - slider.offsetLeft;
            const walk = (x - startX) * 2;
            slider.scrollLeft = scrollLeft - walk;
        }, {passive: true});
    });
</script>

{% schema %}
{
  "name": "In-Store Marquee",
  "settings": [
    { "type": "text", "id": "title", "label": "Heading", "default": "Available In-Store At" },
    { "type": "range", "id": "scroll_speed", "min": 0.5, "max": 3, "step": 0.1, "label": "Scroll Speed", "default": 1.0 }
  ],
  "blocks": [
    {
      "type": "store",
      "name": "Store",
      "settings": [
        { 
          "type": "text", 
          "id": "store_name", 
          "label": "Store Name", 
          "default": "Selfridges & Co" 
        },
        { 
          "type": "image_picker", 
          "id": "logo", 
          "label": "Store Logo (Optional)",
          "info": "If an image is uploaded, it will replace the text name."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "In-Store Marquee",
      "blocks": [
        { "type": "store", "settings": { "store_name": "Selfridges & Co" } },
        { "type": "store", "settings": { "store_name": "la Rinascente" } },
        { "type": "store", "settings": { "store_name": "KaDeWe" } },
        { "type": "store", "settings": { "store_name": "Le Bon Marché" } },
        { "type": "store", "settings": { "store_name": "Harrods" } }
      ]
    }
  ]
}
{% endschema %}